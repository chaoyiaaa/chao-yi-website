# Local dev tooling (not part of the deployed site)
.claude/
serve.ps1

# Third-party reference screenshot (not our content, not used by the site)
website inspo 1.JPG

# Raw source art (large, unused by the site — assets/images/dragon-bg.png
# is the processed version actually referenced in CSS)
dragonbackground.png
